<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Example</h5>
          {{!-- FOR The created list. Make a new file! --}}
          {{#if list = "true"}}
          <p>
            <strong>ID</strong>: {{names.id}}</p>
          <p>
          <p>
            <strong>Text</strong>: {{names.name}}</p>
          <p>
          </p>
          {{/if}}

          {{!-- FOR NAME --}}
           {{#each origins}}
         
              
             <p>
               <strong>Origin</strong>: {{this.origin}}</p>

                <p>
               <strong>Gender</strong>: {{this.origingender}}</p>

                 {{/each}}
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
