<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Saved Names</h5>
              
             
              {{#each names}}
               {{#if list}}
              <li data-id="{{this.id}}" class="list-group-item">
                <a href="name/{{this.id}}">{{this.name}}</a>
              </li>
                 {{/if}}
              {{/each}}

           
          {{!-- {{#if list = "true"}}
          <p>
            <strong>ID</strong>: {{names.id}}</p>
          <p>
          <p>
            <strong>Text</strong>: {{names.name}}</p>
          <p>
          </p>
          {{/if}} --}}

            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
